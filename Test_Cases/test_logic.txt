num start() {
    flag is_valid = true;
    num status = 200;

    check (is_valid == true && status == 200) {
        show(1); 
    } otherwise {
        show(0);
    }
    
    flag is_error = false;
    check (is_error != true || status < 100) {
        show(50); 
    }

    return 0;
}
