num start() {
    num count = 5;
    num factorial = 1;

    
    while (count > 0) {
        factorial = factorial * count;
        count--;

        show("hello\n");
    }

    show(factorial); 
    return 0;
}
